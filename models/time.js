class Time {
  constructor(id, title, seconds, workspace, user) {
    this.id = id,
    this.title = title;
    this.seconds = seconds;
    this.workspace = workspace;
    this.user = user;
  }
}

module.exports = Time
